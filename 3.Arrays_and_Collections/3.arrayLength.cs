using System;

namespace Arrays_and_Collections
{
    class arrayLength
    {
        public static void Run()
        {
            int[] arr = { 1, 2, 3, 4, 5 };
            Console.WriteLine(arr.Length);
        }
    }
}